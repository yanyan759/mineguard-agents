# GitHub 源码发布计划

目标：将当前项目作为 `yanyan759/mineguard-agents` 公开仓库发布，保留可复现的源码、配置和说明。

本次直接在已授权工作区执行发布准备；不修改运行时业务行为。文件范围为 `agent1/`，不包含上层历史参考工程。

- [x] 检查候选文件大小与凭据；排除本机缓存、临时脚本、大型外部研究档案和需单独获取的学位论文。
- [x] 补充项目根目录开发指引、具体 clone 命令和外部资料获取说明。
- [x] 核验暂存文件、文档链接和源码凭据扫描结果；首次提交包含 607 个文件，无超限文件、运行账本或本地凭据。
- [ ] 完成 GitHub 账号授权，创建公开仓库并推送。
- [ ] 比对远端分支与本地提交，检查 README、Compose 与环境模板已上传。

验证沿用当前 69 项单测、5 项 fixture、41 项工具校验和前端构建结果；本次仅变更发布文档和 Git 排除规则，不重复执行无关业务测试。
