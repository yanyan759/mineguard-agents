# 阶段 7「联调与第一版验收」实施计划

> **执行基准**：`personal_task_progress.md` 阶段 7 目标/任务清单/验收标准。
> **用户决策（2026-08-07）**：MySQL=启动 Docker；RAGFlow=仅写导入脚本；Tavily=用户提供 Key；成员 1=保持离线降级。

**Goal**：完成数据环境部署（MySQL 实库、RAGFlow 导入脚本、Tavily 引导）与第一版联调验收（与成员 1 适配、知识检索质量、多会话隔离、取消/重连/异常、审批不绕过、7 场景端到端）。

**Architecture**：MySQL 用 agent1 独立 compose（端口 3307）；RAGFlow 仅产出脚本（离线验证用 fixture YAML）；成员 1 通过 `PREDICTION_MODEL_URL` 环境变量切换 offline/online 双路径；7 场景验收经 WorkflowRunner + FastAPI TestClient 全链路驱动。

**Tech Stack**：Docker / mysql:8.4 / mysql.connector / FastAPI TestClient / langgraph / requests

## 全局约束

- 只改 `agent1/`，禁止改 `code/`
- 新逻辑加中文注释
- 进度日志是唯一基准，不扩大需求边界
- 遇环境阻断必须提问，不静默降级
- 安全约束：SQL 只读白名单、知识带来源、高风险动作人工审批

---

### 任务 1：MySQL 数据环境（DDL + 种子数据 + 部署 + 真实验证）

**Files**:
- Create: `deploy/mysql/docker-compose.yaml`
- Create: `deploy/mysql/sql/01_create_tables.sql`
- Create: `deploy/mysql/sql/02_seed_data.sql`
- Create: `deploy/mysql/scripts/seed_from_yaml.py`
- Create: `tests/validate_mysql_phase7.py`
- Modify: `.env`（MYSQL_PASSWORD 对齐）

**步骤**:
1. 写 5 张表 DDL：accident_cases / personnel / materials_inventory / equipment / work_orders
2. 用 `seed_from_yaml.py` 从 `tests/fixtures/accident_cases.yaml` 生成事故案例 INSERT，其余表手工构造种子
3. 启动 Docker Desktop + `docker compose up -d`（端口 3307，root 密码对齐 .env）
4. `validate_mysql_phase7.py` 验证：连接 + SHOW TABLES 5 表 + 各表行数 + **写操作被拒（DELETE/UPDATE/INSERT → rejected）**
5. 验证命令：`cd agent1 && python tests/validate_mysql_phase7.py`

### 任务 2：RAGFlow 导入脚本（离线验证）

**Files**:
- Create: `deploy/ragflow/scripts/build_documents.py`
- Create: `deploy/ragflow/scripts/ragflow_import.py`
- Create: `deploy/ragflow/README.md`

**步骤**:
1. `build_documents.py`：accident_cases.yaml/regulations.yaml → 3 类 markdown 文档（safety_regulations/accident_cases/support_schemes）
2. `ragflow_import.py`：REST API create dataset → upload document → create assistant（3 助手绑知识库）
3. 验证：离线（fixture YAML 走知识检索质量验收），脚本 dry-run 校验

### 任务 3：成员 1 联调适配（offline/online 双路径）

**Files**:
- Modify: `tools/prediction_model_tool.py`

**步骤**:
1. 读取 `PREDICTION_MODEL_URL`；配置时走真实 HTTP（构造 `ModelInferenceRequest` → POST → 解析 `ModelInferenceResponse`，超时降级 rule_based）
2. 新增 `check_model_health` 工具（GET `/health` → `ModelHealthResponse`）
3. 无 URL 保持 offline 降级；验证契约与降级路径

### 任务 4：Tavily 配置引导

**Files**: Modify: `.env.example`（注释）+ `.env`

**步骤**: 用户提供 Key 填入 `.env`；validate_phase7 检测 key → 真实搜索；空 key 提示 + 离线

### 任务 5：阶段 7 验收脚本

**Files**: Create: `tests/validate_phase7.py`

**步骤**:
1. **知识检索质量**：fixture 路径，校验相似案例/规程/支护方案均带来源；missing_knowledge → 置信度降低 + 专家会商提示
2. **多会话隔离**：2 个 thread_id 并发工作流，事件/状态互不干扰
3. **取消/异常**：cancel 接口、无效场景 400、重复审批 409、不存在 run 404
4. **审批不绕过**：red 必须 waiting_human，批准→completed(dispatched)，拒绝→rejected 终止
5. **7 场景端到端**：normal/yellow/red/missing_knowledge/resource_insufficient/reflection_rollback/max_iterations 逐场景断言日志验收标准

### 任务 6：全量回归 + 日志更新

**步骤**:
1. 阶段 1/3/4/5 + bootstrap 全量回归
2. 更新 `personal_task_progress.md` 阶段 7 状态、改动汇总、验收结果
3. 输出交付汇总（改动清单、关键代码说明、验证方法）
